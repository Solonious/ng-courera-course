import { RuneConverterPipe } from './rune-converter.pipe';

describe('RuneConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new RuneConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
