import { SkranjiFontDirective } from './skranji-font.directive';

describe('SkranjiFontDirective', () => {
  it('should create an instance', () => {
    const directive = new SkranjiFontDirective();
    expect(directive).toBeTruthy();
  });
});
